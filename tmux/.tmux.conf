# Enable modified keys such as Shift+Enter in supported terminals.
set -g extended-keys on

# Number windows from 1 and close numbering gaps when a window is removed.
set -g base-index 1
set -g renumber-windows on
